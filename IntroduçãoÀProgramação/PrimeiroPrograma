#include <stdio.h>

int main() {

	int num1, num2, result; // Declaração de variáveis
	
	scanf("%d", &num1); // Entrada
	scanf("%d", &num2);
	
	result = num1 + num2; // Processamento
	
	printf("%d", result); // Saída
	
	return 0;
	
}